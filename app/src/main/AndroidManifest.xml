<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.dictionary_eng_ja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity_Intro"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Meaning_Activity"
            android:theme="@style/NoActionBar"
            android:label="Meaning"
            android:parentActivityName=".MainActivity_Intro">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity_Intro" />
        </activity>


        <activity android:name=".Search_Activity"
            android:theme="@style/NoActionBar"
            android:label="Search"
            android:parentActivityName=".MainActivity_Intro">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity_Intro" />
        </activity>



    </application>

</manifest>